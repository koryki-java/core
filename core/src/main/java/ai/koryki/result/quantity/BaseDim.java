package ai.koryki.result.quantity;

/** Base dimensions of the quantity calculus - the axes of a {@link DimVector}. */
public enum BaseDim {

    MONEY,
    LENGTH,
    MASS,
    TIME,
    TEMPERATURE,
    COUNT
}

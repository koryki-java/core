// string_agg is an aggregate: it must trigger GROUP BY inference for the
// scalar columns and must not itself be added to GROUP BY.

FIND customers c, c orders o
FETCH c.company_name, string_agg(o.ship_city, ',')

// Pattern-matching chapter: regexp_like maps to DuckDB regexp_matches,
// regexp_count stays under its canonical name.

FIND kunden c
FILTER regexp_count(c.firma, 'a') > 1
FETCH c.firma, regexp_like(c.firma, '^A')
